apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-service-b-to-postgres
  namespace: default
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: postgres-ha
  policyTypes:
    - Ingress
  ingress: []
