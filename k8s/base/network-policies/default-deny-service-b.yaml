apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-service-b
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: service-b
  policyTypes:
  - Ingress
  - Egress
  ingress: []
  egress: []
