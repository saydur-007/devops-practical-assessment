apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-service-a
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: service-a
  policyTypes:
  - Ingress
  - Egress
  ingress: []
  egress: []
