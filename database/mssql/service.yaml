apiVersion: v1
kind: Service
metadata:
  name: mssql
  namespace: default
  labels:
    app: mssql
spec:
  type: ClusterIP
  selector:
    app: mssql
  ports:
    - name: mssql
      port: 1433
      targetPort: 1433
      protocol: TCP
