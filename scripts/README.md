# Scripts and Automation

Deployment automation is implemented through the root `Makefile`.

Primary commands:

```bash
make bootstrap
make verify
```

Operational verification procedures are documented in the main `README.md`.
